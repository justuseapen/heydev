# HeyDev Environment Variables
# Copy this file to .env and fill in your values

# ===========================================
# Server Configuration
# ===========================================

# Port the server listens on (default: 3000)
PORT=3000

# Node environment (production recommended for Docker)
NODE_ENV=production

# ===========================================
# API Keys (Required for full functionality)
# ===========================================

# OpenAI API Key - Required for voice transcription fallback (Whisper API)
# Get yours at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Resend API Key - Required for email notifications and magic link auth
# Get yours at: https://resend.com/api-keys
RESEND_API_KEY=re_your-resend-api-key-here

# ===========================================
# Email Configuration
# ===========================================

# From address for outgoing emails (magic links, notifications)
# Must be a verified domain in Resend, or use onboarding@resend.dev for testing
EMAIL_FROM=HeyDev <noreply@yourdomain.com>

# ===========================================
# Dashboard Configuration
# ===========================================

# URL where the dashboard is hosted (for CORS)
# This should match where you deploy the dashboard
DASHBOARD_URL=http://localhost:5174

# ===========================================
# Development Only (not needed for Docker)
# ===========================================

# These are typically set by docker-compose for production
# DATABASE_URL is not needed - SQLite stores in /app/server/data/heydev.db
